# 大模型调用（OpenAI 兼容 API）
# 复制本文件为 .env.local 后填写真实值；.env.local 不会被提交到 Git。
#
# 优先级：应用内「设置」里填写的 Key > 以下环境变量

# 必填：API Key（OpenAI 或兼容服务商）
OPENAI_API_KEY=

# 可选：API 根地址，默认 https://api.openai.com/v1
# DeepSeek（OpenAI 兼容）：二选一即可
# OPENAI_BASE_URL=https://api.deepseek.com
# OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_BASE_URL=https://api.openai.com/v1

# 可选：默认模型名，未设置时为 gpt-4o
OPENAI_MODEL=gpt-4o

# 可选：站点公网地址（用于 OG/Twitter 等 meta 的绝对 URL；不填则本地用 localhost，Vercel 用部署域名）
# NEXT_PUBLIC_SITE_URL=https://你的域名

# 可选：顶栏 GitHub 图标指向的仓库（不填则默认女娲.skill 方法论仓库）
# NEXT_PUBLIC_GITHUB_REPO_URL=https://github.com/你的账户/蒸人堂
